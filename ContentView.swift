import SwiftUI

struct ContentView: View {
    var body: some View {
       Text("Veer Patel")
        Text("Riddick Variano")
        Image(systemName: "pencil.line")
        
        Image(systemName: "pencil.tip")
    }
}
